# 后端接口缺失与风险清单

审查对象：`D:\codex\zlzc-master`。本清单只记录问题，未修改后端代码。

## P0：权限与流程安全

1. **项目详情缺少数据范围校验**
   `GET /management/project/{projectId}` 直接调用 `selectZProjectById`，未经过 `@DataScope` 或 `checkProjectDataScope`。拥有项目菜单权限的用户可能通过枚举 ID 读取权限范围外的项目。

2. **项目修改允许客户端直接修改状态**
   `PUT /management/project` 的 Mapper 会在 `status != null` 时更新状态。当前没有独立的状态流转/审核服务，客户端可能直接把状态改为 `3`（已通过）。建议普通编辑接口忽略状态，并由受控的生成、提交审核和审核决策接口推进状态。

## P1：完成业务闭环所需接口

1. **创建项目返回项目 ID**
   Mapper 已通过 `useGeneratedKeys` 写回 `projectId`，但 Controller 只返回受影响行数。建议响应 `data.projectId`，避免前端创建后再次查询最新记录。

2. **城市字段**
   `z_project` 与 `ZProject` 没有 `cityCode/cityName`。当前前端把城市以 `【城市：杭州】` 前缀暂存在 `description` 中。建议增加标准行政区划代码字段，至少覆盖浙江 11 个设区市。

3. **生成任务与进度**
   - 创建/启动生成任务
   - 查询任务与节点进度
   - 取消、重试、单镜头重生成
   - SSE 或 WebSocket 进度推送
   - 获取脚本、分镜、音频、视频和最终版本

4. **审核中心**
   - 提交审核
   - 风险项列表、来源证据与处理状态
   - 通过/驳回及审核意见
   - 审核记录

5. **素材与知识资产**
   - 图片/视频上传与分页查询
   - 素材标签、来源、版权证明和授权期限
   - 权威事实卡/知识包检索
   - 项目素材关联和溯源清单

6. **导出中心**
   - 创建导出任务、查询进度、取消/重试
   - 多平台画幅与编码参数
   - 字幕、封面、版权清单生成
   - 交付包下载地址与有效期

## P2：后台配套能力

- 通知列表、已读状态与实时推送
- 组织生成配额、用量统计与模型路由配置
- 城市库配置持久化
- 手机号验证码登录（当前后端仅支持账号、密码、图形验证码）
- 编辑越权错误文案目前复用“没有权限删除该项目”，建议按操作类型区分
